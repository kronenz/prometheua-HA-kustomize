# 네임스페이스별 리소스 사용량 대시보드 생성 완료

## 📊 개요
쿠버네티스 네임스페이스별 CPU, 메모리, 디스크, 네트워크 사용량을 통합 모니터링하는 대시보드 생성

**Dashboard UID**: `namespace-resources-v1`
**Dashboard 이름**: 쿠버네티스 네임스페이스별 리소스 사용량
**작업 일자**: 2025-11-11
**상태**: ✅ 완료

---

## 🎯 Dashboard 구성

### 패널 구성 (총 10개 패널)

#### 1. 📊 Overview 섹션 (2개 패널)
1. **💻 네임스페이스별 CPU 사용량 (Cores)**
   - Type: Time Series
   - 네임스페이스별 CPU 코어 사용량 실시간 추이

2. **🧠 네임스페이스별 메모리 사용량 (Bytes)**
   - Type: Time Series
   - 네임스페이스별 메모리 사용량 실시간 추이

#### 2. 🌐 네트워크 사용량 섹션 (2개 패널)
3. **📥 네트워크 수신 속도 (Bytes/sec)**
   - Type: Time Series
   - 네임스페이스별 네트워크 인바운드 트래픽

4. **📤 네트워크 송신 속도 (Bytes/sec)**
   - Type: Time Series
   - 네임스페이스별 네트워크 아웃바운드 트래픽

#### 3. 💾 디스크 사용량 섹션 (4개 패널)
5. **📀 PV 사용량 (Bytes)**
   - Type: Time Series
   - Persistent Volume 사용량

6. **📊 PV 사용률 (%)**
   - Type: Time Series
   - PV 전체 용량 대비 사용률

7. **📖 디스크 읽기 IOPS**
   - Type: Time Series
   - 초당 디스크 읽기 작업 수

8. **✍️ 디스크 쓰기 IOPS**
   - Type: Time Series
   - 초당 디스크 쓰기 작업 수

#### 4. 📋 요약 섹션 (1개 패널)
9. **📊 네임스페이스별 리소스 사용 현황**
   - Type: Table
   - 모든 네임스페이스의 리소스 사용량을 한눈에 확인
   - 컬럼:
     - 네임스페이스
     - CPU (Cores)
     - 메모리 (Bytes)
     - 네트워크 수신 (B/s)
     - 네트워크 송신 (B/s)
     - PV 사용량 (Bytes)
     - PV 사용률 (%) - Color-coded (Green/Yellow/Red)

---

## 📈 PromQL 쿼리

### CPU 사용량
```promql
# 네임스페이스별 CPU 사용량 (Cores)
sum(rate(container_cpu_usage_seconds_total{container!="",container!="POD",image!=""}[5m])) by (namespace)
```

### 메모리 사용량
```promql
# 네임스페이스별 메모리 Working Set (실제 사용 중인 메모리)
sum(container_memory_working_set_bytes{container!="",container!="POD",image!=""}) by (namespace)
```

### 네트워크 사용량
```promql
# 네트워크 수신 속도 (Bytes/sec)
sum(rate(container_network_receive_bytes_total[5m])) by (namespace)

# 네트워크 송신 속도 (Bytes/sec)
sum(rate(container_network_transmit_bytes_total[5m])) by (namespace)
```

### 디스크 사용량 (PV)
```promql
# PV 사용량 (Bytes)
sum(kubelet_volume_stats_used_bytes) by (namespace)

# PV 사용률 (%)
sum(kubelet_volume_stats_used_bytes) by (namespace)
/ sum(kubelet_volume_stats_capacity_bytes) by (namespace) * 100
```

### 디스크 I/O
```promql
# 디스크 읽기 IOPS
sum(rate(container_fs_reads_total[5m])) by (namespace)

# 디스크 쓰기 IOPS
sum(rate(container_fs_writes_total[5m])) by (namespace)
```

---

## 🔍 현재 수집 중인 메트릭 샘플

### CPU 사용량 (상위 네임스페이스)
```
monitoring:         0.311 cores
kube-system:        0.220 cores
longhorn-system:    0.124 cores
gitlab:             0.058 cores
argocd:             0.029 cores
jenkins:            0.014 cores
fluent-operator:    0.004 cores
spark:              0.003 cores
opensearch:         0.002 cores
```

### 메모리 사용량 (상위 네임스페이스)
```
monitoring:         4.00 GB
kube-system:        1.97 GB
jenkins:            1.49 GB
longhorn-system:    1.21 GB
argocd:             0.87 GB
spark:              0.73 GB
gitlab:             0.12 GB
opensearch:         0.05 GB
fluent-operator:    0.02 GB
```

### 네트워크 사용량 (상위 네임스페이스)

**수신 (RX):**
```
kube-system:        5,303.37 KB/s
monitoring:         1,831.27 KB/s
longhorn-system:      286.75 KB/s
jenkins:              156.80 KB/s
argocd:                42.85 KB/s
gitlab:                16.41 KB/s
fluent-operator:        2.26 KB/s
opensearch:             1.99 KB/s
spark:                  0.38 KB/s
```

**송신 (TX):**
```
monitoring:           254.62 KB/s
kube-system:           63.35 KB/s
jenkins:              132.61 KB/s
longhorn-system:       32.27 KB/s
gitlab:                16.38 KB/s
argocd:                 9.72 KB/s
spark:                  2.08 KB/s
opensearch:             1.16 KB/s
fluent-operator:        0.39 KB/s
```

---

## 🎨 Dashboard 특징

### 1. 한글 UI
- 모든 패널 제목과 범례가 한글로 표시
- 직관적인 이모지 사용으로 가독성 향상

### 2. 실시간 모니터링
- 30초 자동 새로고침
- Time Range: 최근 1시간 (사용자 정의 가능)

### 3. 컬러 코딩
- PV 사용률 패널에 임계값 기반 색상 표시:
  - 🟢 Green: 0-70%
  - 🟡 Yellow: 70-85%
  - 🔴 Red: 85-100%

### 4. 정렬 및 필터링
- 테이블: CPU 사용량 기준 내림차순 정렬
- Legend: Last, Max 값 표시

### 5. Tooltip
- Multi-series 모드
- 내림차순 정렬로 높은 값부터 표시

---

## 📊 사용 사례

### 1. 리소스 사용 분석
- 어느 네임스페이스가 가장 많은 리소스를 사용하는지 파악
- 시간대별 리소스 사용 패턴 분석
- 리소스 사용 추세 예측

### 2. 용량 계획
- 현재 리소스 사용률 기반 스케일링 결정
- PV 용량 부족 사전 감지
- 클러스터 확장 시점 판단

### 3. 비용 최적화
- 과도한 리소스를 할당받은 네임스페이스 식별
- 미사용 리소스 정리
- 네임스페이스별 비용 추적

### 4. 네트워크 트러블슈팅
- 비정상적인 네트워크 트래픽 감지
- 네임스페이스 간 통신 패턴 분석
- DDoS 또는 데이터 유출 의심 상황 탐지

### 5. 성능 모니터링
- 디스크 I/O 병목 지점 파악
- CPU/메모리 리소스 경합 감지
- 애플리케이션 성능 저하 원인 분석

---

## 🚨 권장 알람 설정

### Critical (즉시 대응 필요)
```yaml
# PV 사용률 90% 이상
sum(kubelet_volume_stats_used_bytes) by (namespace)
/ sum(kubelet_volume_stats_capacity_bytes) by (namespace) * 100 > 90

# 메모리 사용량 특정 임계값 초과 (예: 10GB)
sum(container_memory_working_set_bytes) by (namespace) > 10737418240

# CPU 사용량 급증 (평균 대비 3배 이상)
sum(rate(container_cpu_usage_seconds_total[5m])) by (namespace)
> 3 * avg_over_time(sum(rate(container_cpu_usage_seconds_total[5m])) by (namespace)[1h])
```

### Warning (주의 관찰)
```yaml
# PV 사용률 75% 이상
sum(kubelet_volume_stats_used_bytes) by (namespace)
/ sum(kubelet_volume_stats_capacity_bytes) by (namespace) * 100 > 75

# 네트워크 수신 속도 비정상 (100MB/s 초과)
sum(rate(container_network_receive_bytes_total[5m])) by (namespace) > 104857600

# 디스크 쓰기 IOPS 높음 (1000 IOPS 초과)
sum(rate(container_fs_writes_total[5m])) by (namespace) > 1000
```

---

## 📁 파일 및 배포 정보

### Dashboard JSON
- **파일**: `/tmp/namespace-resources-dashboard.json`
- **ConfigMap**: `grafana-dashboard-namespace-resources-v1`
- **Namespace**: `monitoring`
- **Label**: `grafana_dashboard=1`

### 배포 명령어
```bash
# ConfigMap 생성
kubectl create configmap grafana-dashboard-namespace-resources-v1 \
  --from-file=namespace-resources-v1.json=/tmp/namespace-resources-dashboard.json \
  -n monitoring

# Grafana 라벨 추가
kubectl label cm -n monitoring grafana-dashboard-namespace-resources-v1 grafana_dashboard=1
```

---

## 🔧 커스터마이징 가이드

### 1. Time Range 변경
Dashboard JSON에서 `time` 섹션 수정:
```json
"time": {
  "from": "now-6h",  // 6시간으로 변경
  "to": "now"
}
```

### 2. 새로고침 간격 변경
```json
"refresh": "1m",  // 1분으로 변경
```

### 3. 특정 네임스페이스 필터링
쿼리에 필터 추가:
```promql
sum(rate(container_cpu_usage_seconds_total{
  container!="",
  container!="POD",
  namespace=~"monitoring|kube-system"  # 특정 네임스페이스만
}[5m])) by (namespace)
```

### 4. 단위 변경
- CPU: `cores` → `percentunit` (백분율로 표시)
- Memory: `bytes` → `decgbytes` (GB로 표시)
- Network: `Bps` → `MBs` (MB/s로 표시)

### 5. 추가 메트릭
```promql
# Pod 개수
count(kube_pod_info) by (namespace)

# Deployment 개수
count(kube_deployment_spec_replicas) by (namespace)

# Container 재시작 횟수
sum(rate(kube_pod_container_status_restarts_total[5m])) by (namespace)
```

---

## 📊 메트릭 소스

### Container Metrics (cAdvisor)
- `container_cpu_usage_seconds_total`: CPU 사용 시간 (초)
- `container_memory_working_set_bytes`: 메모리 Working Set
- `container_network_receive_bytes_total`: 네트워크 수신 바이트
- `container_network_transmit_bytes_total`: 네트워크 송신 바이트
- `container_fs_reads_total`: 파일시스템 읽기 횟수
- `container_fs_writes_total`: 파일시스템 쓰기 횟수

### Kubelet Metrics
- `kubelet_volume_stats_used_bytes`: PV 사용량
- `kubelet_volume_stats_capacity_bytes`: PV 전체 용량

---

## 🔍 트러블슈팅

### 1. 특정 네임스페이스가 표시되지 않을 때
**원인**: 해당 네임스페이스에 실행 중인 Pod가 없음

**확인**:
```bash
kubectl get pods -n <namespace>
```

### 2. PV 메트릭이 없을 때
**원인**: PVC를 사용하는 Pod가 없음

**확인**:
```bash
kubectl get pvc --all-namespaces
kubectl get pv
```

### 3. 네트워크 메트릭이 0일 때
**원인**:
- Pod에 네트워크 트래픽이 없음
- cAdvisor가 네트워크 메트릭을 수집하지 못함

**확인**:
```bash
# cAdvisor 메트릭 확인
kubectl exec -n monitoring prometheus-kube-prometheus-stack-prometheus-0 -- \
  wget -qO- 'http://localhost:9090/api/v1/query?query=container_network_receive_bytes_total'
```

### 4. Dashboard가 로드되지 않을 때
**확인 사항**:
```bash
# ConfigMap 존재 확인
kubectl get cm -n monitoring grafana-dashboard-namespace-resources-v1

# 라벨 확인
kubectl get cm -n monitoring grafana-dashboard-namespace-resources-v1 \
  -o jsonpath='{.metadata.labels}'

# Grafana Pod 재시작
kubectl rollout restart deployment -n monitoring grafana
```

---

## 💡 Best Practices

### 1. 정기적인 리소스 검토
- 주간 리소스 사용 리포트 생성
- 트렌드 분석으로 용량 계획 수립
- 미사용 리소스 정리

### 2. 임계값 설정
- 환경에 맞는 알람 임계값 조정
- False Positive 최소화
- 점진적 임계값 강화

### 3. 네임스페이스 설계
- 목적별 네임스페이스 분리 (dev, staging, prod)
- 리소스 쿼터 설정
- LimitRange로 Pod 리소스 제한

### 4. 태그 활용
- Dashboard에 환경 태그 추가
- 팀별, 프로젝트별 Dashboard 복제
- 버전 관리

### 5. 문서화
- 각 네임스페이스의 용도 문서화
- 리소스 할당 기준 정리
- 알람 대응 절차 작성

---

## 🎯 향후 개선 사항

### 1. Variable 추가
```json
{
  "name": "namespace",
  "type": "query",
  "query": "label_values(kube_namespace_created, namespace)",
  "multi": true,
  "includeAll": true
}
```

### 2. S3 스토리지 모니터링
- MinIO 메트릭 통합
- 네임스페이스별 S3 버킷 사용량
- S3 API 호출 통계

### 3. 비용 추적
- CPU/메모리 단가 정의
- 네임스페이스별 비용 계산
- 월별 비용 트렌드

### 4. 예측 분석
- 머신러닝 기반 리소스 예측
- 용량 부족 사전 경고
- 자동 스케일링 권장

### 5. 통합 대시보드
- ArgoCD 배포 이벤트 오버레이
- Jenkins 빌드 시간과 리소스 사용량 상관관계
- 로그 이벤트 통합

---

## ✅ 완료 체크리스트

- [x] Dashboard JSON 생성
- [x] PromQL 쿼리 작성 및 테스트
- [x] ConfigMap 배포
- [x] Grafana 라벨 설정
- [x] 메트릭 수집 검증
- [x] 한글 UI 적용
- [x] 컬러 코딩 설정
- [x] 문서화 완료

---

## 📞 접속 정보

- **Grafana URL**: http://grafana.k8s-cluster-01.miribit.lab
- **Dashboard UID**: `namespace-resources-v1`
- **Dashboard 이름**: 쿠버네티스 네임스페이스별 리소스 사용량

---

**작업 완료 시각**: 2025-11-11 14:35 UTC
**작업자**: Claude (Anthropic)
**검증 상태**: ✅ 완료
