---
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cm
  namespace: argocd
data:
  # Enable exec for debugging (optional)
  exec.enabled: "true"

  # Repository credentials template (will be configured after GitLab deployment)
  # repositories: |
  #   - url: http://gitlab.k8s-cluster-01.miribit.lab/observability/thanos-multi-cluster.git
  #     passwordSecret:
  #       name: gitlab-repo-creds
  #       key: password
  #     usernameSecret:
  #       name: gitlab-repo-creds
  #       key: username

  # Multi-cluster configuration
  # Additional cluster secrets will be added for cluster-02, cluster-03, cluster-04
