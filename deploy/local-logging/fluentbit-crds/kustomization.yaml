apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: logging

resources:
  - namespace.yaml
  - serviceaccount.yaml
  - lua-scripts-configmap.yaml
  - fluentbitconfig.yaml
  - clusterparser.yaml
  - clusterinput-containers.yaml
  - clusterinput-hostpath.yaml
  - clusterfilter.yaml
  - clusteroutput.yaml

# FluentBit CR은 fluent-operator-system 네임스페이스에 배포 (별도 관리)
# kustomize build 후 아래 명령으로 배포:
# kubectl apply -f fluentbit.yaml
